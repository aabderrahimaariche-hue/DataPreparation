% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11_categorical_intelligence.R
\name{encode_categorical_flags}
\alias{encode_categorical_flags}
\title{Encode Categorical Data into Binary Flags (One-Hot Encoding)}
\usage{
encode_categorical_flags(
  data = NULL,
  categorical_col,
  identifier_col = NULL,
  delimiter = "\\\\\\\\|",
  prefix = NULL,
  dataset_name = NULL
)
}
\arguments{
\item{data}{A data frame containing categorical column (optional if dataset_name provided)}

\item{categorical_col}{Column name with delimited categories (e.g., "genres")}

\item{identifier_col}{Optional: ID column to preserve unique records}

\item{delimiter}{Pattern to split categories (default: "\\\\|" for pipe)}

\item{prefix}{Optional: prefix for output columns (default: category name)}

\item{dataset_name}{Character. Name of registered dataset to use.}
}
\value{
Data frame with one-hot encoded binary columns (0/1 for each category)
}
\description{
Convert delimited categorical data (PIPE|COMMA|SEMICOLON) into binary indicator columns.
Works with ANY categorical structure: genres|tags|categories|attributes
}
\examples{
\dontrun{
# MovieLens: encode genres
genre_flags <- encode_categorical_flags(data, "genres", "movieId", "\\\\\\\\|")

# E-commerce: encode product tags
tag_flags <- encode_categorical_flags(data, "tags", "productId", ",")

# Healthcare: encode symptoms
symptom_flags <- encode_categorical_flags(data, "symptoms", "patientId", ";")
}

}
