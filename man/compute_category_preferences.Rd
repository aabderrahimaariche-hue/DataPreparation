% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11_categorical_intelligence.R
\name{compute_category_preferences}
\alias{compute_category_preferences}
\title{Compute Entity-Category Preferences}
\usage{
compute_category_preferences(
  data,
  identifier_col,
  categorical_col,
  value_col,
  delimiter = "\\\\\\\\|"
)
}
\arguments{
\item{data}{A data frame with identifier, categories, and values}

\item{identifier_col}{Column name with entity identifiers (e.g., "userId")}

\item{categorical_col}{Column name with delimited categories (e.g., "genres")}

\item{value_col}{Column name with numeric values (e.g., "rating")}

\item{delimiter}{Pattern to split categories (default: "\\\\|")}
}
\value{
Data frame with one row per entity showing preference per category
  (pivot_wider format: entity_id + pref_category1 + pref_category2 + ...)
}
\description{
Calculate preference deviation (from global mean) for each identifier-category pair.
Used to find what categories each entity prefers based on valeurization values.
}
\examples{
\dontrun{
# User genre preferences: which genres does each user prefer?
user_prefs <- compute_category_preferences(data, "userId", "genres", "rating", "\\\\\\\\|")

# Customer product preferences
customer_prefs <- compute_category_preferences(data, "customerId", "product_type", "purchase_value", ",")
}

}
