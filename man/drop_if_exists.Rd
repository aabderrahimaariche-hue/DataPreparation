% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/15_drop_if_exists.R
\name{drop_if_exists}
\alias{drop_if_exists}
\title{Drop Dataset if It Exists (Safe Removal)}
\usage{
drop_if_exists(dataset_names, verbose = TRUE)
}
\arguments{
\item{dataset_names}{Character vector or single name: datasets to remove}

\item{verbose}{Logical: print status messages? (default: TRUE)}
}
\value{
Invisible logical (always TRUE)
}
\description{
Safely removes datasets without throwing errors if they don't exist.
Useful for cleanup code where you're not sure which datasets are in memory.
}
\examples{
\dontrun{
# Drop datasets safely (no error if missing)
drop_if_exists(c('data1', 'missing_data', 'temp'))
# Drops data1 and temp, silently skips missing_data
}

}
