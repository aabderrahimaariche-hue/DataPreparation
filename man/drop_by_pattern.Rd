% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/16_drop_by_pattern.R
\name{drop_by_pattern}
\alias{drop_by_pattern}
\title{Drop Datasets Matching a Pattern}
\usage{
drop_by_pattern(pattern, verbose = TRUE, confirm = TRUE)
}
\arguments{
\item{pattern}{Regex pattern to match (e.g., "^temp_" for temp_x, temp_y, etc.)}

\item{verbose}{Logical: print status messages? (default: TRUE)}

\item{confirm}{Logical: ask for confirmation before dropping? (default: TRUE)}
}
\value{
Invisible logical. TRUE if dropped, FALSE if cancelled or no matches.
}
\description{
Removes all datasets whose names match a regex pattern.
Useful for cleanup if you consistently name temporary datasets (e.g., "temp_*")
}
\examples{
\dontrun{
# Drop all datasets starting with "temp_"
drop_by_pattern("^temp_")

# Drop all datasets ending with "_old"
drop_by_pattern("_old$", confirm = FALSE)  # No confirmation needed
}

}
